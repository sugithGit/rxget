name: counter_example
description: A counter example app demonstrating rxget_generator code generation.
publish_to: none
version: 1.0.0

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter
  rxget:
    path: ../../packages/rxget
  rxget_annotation:
    path: ../../packages/rxget_annotation

dev_dependencies:
  build_runner: ">=2.0.0 <3.0.0"
  flutter_test:
    sdk: flutter
  rxget_generator:
    path: ../../packages/rxget_generator
