name: hooks_rxget
description: "A new Flutter package project."
version: 0.0.1

environment:
  sdk: ^3.10.0
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3+1
  rxget:
    path: ../rxget

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
