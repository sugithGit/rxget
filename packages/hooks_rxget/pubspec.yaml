name: hooks_rxget
description: "A Flutter package that provides hooks for the rxget state management library."
version: 0.0.1
homepage: https://github.com/sugithGit/rxget

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=1.17.0"

dependencies:
  flutter:
    sdk: flutter
  flutter_hooks: ^0.21.3+1
  rxget: ^0.1.0+3

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
