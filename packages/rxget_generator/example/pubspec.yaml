name: rxget_generator_example
description: An example demonstrating how to use rxget_generator to generate GetxState classes.
version: 0.0.1
publish_to: none

environment:
  sdk: ">=3.9.0 <4.0.0"

dependencies:
  rxget_annotation: ^0.0.1

dev_dependencies:
  build_runner: ">=2.15.0"
  rxget_generator:
    path: ../
