## 1.0.0+2

* Migrate to new Dart analyzer element model APIs.
* Add documentation for public APIs and classes.
* Fix repository and homepage URL.
* Add working example.

## 0.0.1

* Initial release. Generates safe `_State` classes with properly closing `.obs` fields and exposes clean public APIs.
