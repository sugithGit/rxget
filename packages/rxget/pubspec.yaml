name: rxget
description: A lightweight reactive state management and dependency injection library for Dart and Flutter — pure and minimal.
version: 0.1.1
homepage: https://github.com/sugithGit/rxget

environment:
  sdk: ">=3.9.0 <4.0.0"
  flutter: ">=3.22.0"

dependencies:
  flutter:
    sdk: flutter

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
